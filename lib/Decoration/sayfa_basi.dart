import 'package:flutter/material.dart';

class SayfaBasi extends StatelessWidget {
  const SayfaBasi({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Container(
        width: double.infinity, height: 140, color: Colors.transparent);
  }
}
